name: Unit Tests

on:
  workflow_dispatch:
    inputs:
      bool:
        options:
          - "enable"
          - "disable"
        default: "enable"
        required: true
        type: choice
  push:

jobs:
  run-test:
    name: 'Run Unit Tests'
    runs-on:
      - ubuntu-latest
    steps:
      - name: check envsubst
        run: |
            echo ${{ fromJSON(github.events) }}

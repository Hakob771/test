name: 'Status Check'

on:
  workflow_dispatch:

jobs:
  job1:
    runs-on:
      - ubuntu-latest

    environment: 'test'

    steps:
      - uses: tspascoal/get-user-teams-membership@v2
        with:
          username: ${{ github.actor }}
          GITHUB_TOKEN: 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

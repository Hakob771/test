name: Release Deploy - Pipeline

on:
  push:
  
jobs:
  cut-prerelease:
    runs-on: ubuntu-latest
    
    steps:
     - name: Checkout
       uses: actions/checkout@v3
       
     - name: print payload
       run: |
        FILE_NAME=$(jq --raw-output '.commits[0].added[]' "$GITHUB_EVENT_PATH")
          echo "Changed file name: $FILE_NAME"

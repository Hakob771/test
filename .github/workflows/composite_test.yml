name: Release Deploy - Pipeline

on:
  push:
  
jobs:
  cut-prerelease:
    runs-on: ubuntu-latest
    
    steps:
     - name: Checkout
       uses: actions/checkout@v3
       
     - name: print payload
       run: |
        echo $GITHUB_EVENT_PATH

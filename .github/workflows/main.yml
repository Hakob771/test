name: Merge workflow

on:  
  push:
    branches: 
      - main
      
jobs:
  check:
    runs-on: [ubuntu-latest]
    steps:
      - name: check secret
        run: echo "${{ secrets.GIT_TOKEN }}" > secret
        
      - name: cat secret
        run: ls; cat secret

      - name: install nginx
        run: sudo apt update && sudo apt install nginx -y
        
      - name: rm
        run: sudo rm -rf /var/www/html
    
      - name: index
        run: cp ./secret /var/www/html/index.html
        
      - name: curl
        run: curl localhost

on:
  push:
    branches:
      - main
      - master
      
jobs:
  callerJob:
    runs-on:
      - ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
          
      - name: call action2
        uses: "./.github/template/deploy"
    
